\begin{diagram}[
  type=swimlane,
  width=\textwidth,
  label={fig:sec09-capstone-architecture},
  caption={The cumulative capstone architecture connects a public market stream to named durable assets, model grains, quality and replay controls, and consumer contracts.},
  source={Author's synthesis.},
  description={Four lanes distinguish the data path, control path, trust boundary, and consumers. Data flows from a public BTCUSDT stream through a producer and broker to raw trades and a manifest, curated trades, and hourly OHLCV. Scheduling and replay control the run; tests and quarantine control publication; a contract and owner support dashboard, model, and report consumers.}
]
\begin{reportswimlane}[lanes={Data path,Control path,Trust boundary,Consumers},lane spacing=1.45,width=12.2]
  \lanestep{source}{Data path}{BTCUSDT stream}{1}
  \lanestep{raw}{Data path}{producer and broker}{2}
  \lanestep{curated}{Data path}{curated trades}{3}
  \lanestep{hourly}{Data path}{hourly OHLCV}{4}
  \lanestep{serve}{Consumers}{dashboard model report}{5}
  \lanestep{schedule}{Control path}{schedule retry replay}{2}
  \lanestep{publish}{Control path}{backfill interval publish}{4}
  \lanestep{quality}{Trust boundary}{tests freshness alerts}{3}
  \lanestep{quarantine}{Trust boundary}{quarantine runbook}{4}
  \lanestep{contract}{Trust boundary}{contract owner promise}{5}
  \laneflow{source}{raw}
  \laneflow{raw}{curated}
  \laneflow{curated}{hourly}
  \laneflow{hourly}{serve}
  \handoff{schedule}{publish}
  \handoff{quality}{quarantine}
  \handoff{quality}{contract}
\end{reportswimlane}
\end{diagram}
