\begin{diagram}[
  type=architecture,
  label={fig:sec08-platform-boundaries},
  caption={Eight platform capability boundaries run from source systems to serving; a single vendor product commonly spans several adjacent boundaries, but ownership, security posture, cost, and exit cost still attach to each boundary separately.},
  source={Author's synthesis.},
  description={Eight ordered capability layers, top to bottom: Source (events, APIs, databases); Transport (producer, broker, raw landing); Storage and tables (Parquet, table format, catalog); Processing (SQL, models, aggregates); Orchestration (schedule, retry, backfill); Quality (tests, freshness, observability); Governance (owner, contract, access policy); Serving (dashboard, feature, application). An annotation gives a concrete example of vendor span: a managed warehouse commonly implements Storage and tables, Processing, and Serving in one product, while Orchestration, Quality, and Governance still need separate configuration and an accountable owner.}
]
\begin{reportarchitecture}[
  width=15.2,
  annotation={Example span: a managed cloud warehouse commonly implements Storage and tables, Processing, and Serving in one product. Orchestration, Quality, and Governance still need separate configuration and an accountable owner.},
  annotation-position=top
]
  \layer{Source}{events, APIs, databases}
  \layer{Transport}{producer, broker, raw landing}
  \layer{Storage and tables}{Parquet, table format, catalog}
  \layer{Processing}{SQL, models, aggregates}
  \layer{Orchestration}{schedule, retry, backfill}
  \layer{Quality}{tests, freshness, observability}
  \layer{Governance}{owner, contract, access policy}
  \layer{Serving}{dashboard, feature, application}
\end{reportarchitecture}
\end{diagram}
